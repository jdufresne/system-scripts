- name: Install PostgreSQL
  dnf:
    name:
      - postgresql
      - postgresql-contrib
      - postgresql-devel
      - postgresql-server
      # TODO: Remove python2-psycopg2 when Python 2 support is dropped.
      - python2-psycopg2  # For postgresql_user
      - python3-psycopg2  # For postgresql_user
  become: yes

- name: Initialize PostgreSQL
  command: postgresql-setup --initdb
  args:
    creates: /var/lib/pgsql/data/postgresql.conf
  become: yes

- name: Copy pg_hba.conf
  copy:
    src: pg_hba.conf
    dest: /var/lib/pgsql/data/pg_hba.conf
    owner: postgres
    group: postgres
    mode: 0600
  become: yes
  notify: Restart PostgreSQL

- name: Create PostgreSQL conf.d directory
  file:
    path: /var/lib/pgsql/data/conf.d
    state: directory
    owner: postgres
    group: postgres
    mode: 0700
  become: yes

- name: Add include directory to postgresql.conf
  lineinfile:
    path: /var/lib/pgsql/data/postgresql.conf
    regexp: "^#?include_dir = 'conf.d'(?P<rest>.*)"
    line: "include_dir = 'conf.d'\\g<rest>"
    backrefs: yes
  become: yes

- name: Copy PostgreSQL configuration
  copy:
    src: "{{ item }}"
    dest: /var/lib/pgsql/data/conf.d/
    owner: postgres
    group: postgres
    mode: 0600
  with_items:
    - nondurable.conf
  become: yes
  notify: Restart PostgreSQL

- name: Enable PostgreSQL server
  systemd:
    name: postgresql
    enabled: yes
    state: started
  become: yes

- name: Add PostgreSQL user
  postgresql_user:
    name: "{{ ansible_user }}"
    role_attr_flags: SUPERUSER
